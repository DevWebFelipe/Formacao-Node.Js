001 -> Criar um 'readme.md' com os requisitos iniciais do projeto

002 -> ('install-packages - 001') Iniciar um projeto node 

003 -> Criar uma pasta 'src' para
  003 -> Criar um arquivo 'server.ts'

004 -> ('install-packages - 002') Instalar as bibliotecas inicias necessárias para o projeto 

005 -> Criar um '.gitignore' para o projeto
  005 -> Já inserir o '.env' 'node_modules' 'build'

006 -> ('install-packages - 003') Iniciar um projeto TS criado o 'tsconfig.json' 
  006 -> Dentro do 'tsconfig.json' já configurar a chave "target" com o valor "es2020"

007 -> ('comminstall-packagesand - 004') Instalar a biblioteca 'fastify' para gerenciar as rotas da aplicação

008 -> Dividir a aplicação em 2 arquivos, criando o arquivo 'app.ts'

009 -> ('package.json') Configurar o script para rodar o servivdor ('command')

010 -> ('package.json') Configurar o script para rodar o build de TS para JS '"build": "tsup src --out-dir build"'